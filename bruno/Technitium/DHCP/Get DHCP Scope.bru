meta {
  name: Get DHCP Scope
  type: http
  seq: 4
}

get {
  url: http://{{host}}/api/dhcp/scopes/get?token={{api_token}}&name=guest
  body: none
  auth: none
}

params:query {
  token: {{api_token}}
  name: guest
}
