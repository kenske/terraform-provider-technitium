meta {
  name: Get DNS Zone
  type: http
  seq: 1
}

get {
  url: http://{{host}}/api/zones/options/get?token={{api_token}}&zone=example.com
  body: none
  auth: none
}

params:query {
  token: {{api_token}}
  zone: example.com
}
